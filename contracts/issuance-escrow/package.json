{
  "name": "@chainstrike/contract-issuance-escrow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "algokit compile typescript src/*.algo.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf src/out"
  },
  "dependencies": {
    "@algorandfoundation/algorand-typescript": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
